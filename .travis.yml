language: ruby
rvm:
    - 1.9.3

branches:
  only:
    - master

script:
    - bundle exec rake db:migrate
    - bundle exec rake spec
    - bundle exec rake spec:javascript
    - bundle exec rake cucumber

cache: bundler

addons:
  code_climate:
    repo_token: 54f76c90e30ba05d6e0021b4
